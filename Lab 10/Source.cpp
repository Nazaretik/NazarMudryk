#include <iostream>
#include <time.h>
using name std;
void main()
{
	float** matrix;
	int rowCount, colCount;
	cout << "Введіть розмір двовимірного масиву" << endl;
	cout << "Кількість рядків: ";
	cin >> rowCount;
	cout<< "Кількість рядків: ";
	cin >> colCount;


	matrix = new float* [rowCount];

	float min = -10, max = 50;
	srand(time(0));
	cout << "Початковий масив: " << endl;
	for (int i = 0; i < rowCout; i++)
	{
		matrix[i] = mew float[colCount];

	}

}